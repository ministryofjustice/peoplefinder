apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: peoplefinder-ingress
  namespace: peoplefinder-staging
spec:
  tls:
    - hosts:
        - peoplefinder-staging.apps.live-1.cloud-platform.service.justice.gov.uk
        - staging.peoplefinder.service.gov.uk
      secretName: peoplefinder-certificate
  rules:
    - host: peoplefinder-staging.apps.live-1.cloud-platform.service.justice.gov.uk #staging.peoplefinder.service.gov.uk
      http:
        paths:
          - path: /
            backend:
              serviceName: peoplefinder-service
              servicePort: 3000
