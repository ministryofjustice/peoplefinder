apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: peoplefinder-ingress
  namespace: peoplefinder-development
spec:
  tls:
    - hosts:
        - peoplefinder-development.apps.live-1.cloud-platform.service.justice.gov.uk
      secretName: peoplefinder-certificate
  rules:
    - host: peoplefinder-development.apps.live-1.cloud-platform.service.justice.gov.uk
      http:
        paths:
          - path: /
            backend:
              serviceName: peoplefinder-service
              servicePort: 3000
