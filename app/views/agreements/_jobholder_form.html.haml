= simple_form_for @agreement, url: jobholder_agreements_path, html: {multipart: true} do |f|
  %fieldset
    = f.error_notification
    .form-group
      = f.input :manager_email, as: :string, required: true

  .actions
    = f.submit 'Save'
