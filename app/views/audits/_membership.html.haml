- membership, verb = (version.destruction?) ? [version.reify, 'was'] : [version.item, 'set to']
- if membership
  %strong Person #{ verb }:
  = membership.person
  %br
  %strong Group #{ verb }:
  = membership.group
  %br
  %strong Title #{ verb }:
  = membership.role
  %br
  %strong Leader #{ verb }:
  = membership.leader? ? 'Yes' : 'No'
