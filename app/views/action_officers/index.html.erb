<h1>Listing Action Officers</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Email</th>
	<th>Active/Inactive</th>
    <th></th>
    <th></th>
  </tr>

  <%  @action_officers.each do |action_officer| %>
    <tr>
      <td><%= action_officer.name %>
      <td><%=  action_officer.email %>
      <td><% if action_officer.deleted %>
            <i class="fa fa-times"></i>Inactive
          <% else %>
            <i class="fa fa-check-circle"></i>Active
          <% end %>
      <td><%= link_to 'Show', action_officer , {class: 'button'} %>
      <td><%= link_to 'Edit', edit_action_officer_path(action_officer), {class: 'button'} %>
    </tr>
  <% end %>

</table>


<%= link_to 'New Action officer', new_action_officer_path , {class: 'button'} %>
