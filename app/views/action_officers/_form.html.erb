<%= form_for @action_officer do |f| %>
  <% if @action_officer.errors.any? %>
    <div class="error_explanation">
      <h2><%= "#{pluralize(@action_officer.errors.count, "error")} prohibited this action_officer from being saved:" %></h2>
      <ul>
        <% @action_officer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <div>
    <%= f.submit 'Save' %>
  </div>

<% end %>