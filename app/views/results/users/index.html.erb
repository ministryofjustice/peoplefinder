<h2>
  <%= t('views.results.direct_reports_title') %>
</h2>

<table>
  <tbody>
    <% @users.each do |user| %>
      <tr class="<%=  cycle('stripe', '')  %>">
        <td class="name">
          <%= link_to(user, [:results, user, :reviews]) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<hr />

<%= link_to_alternative_format(t('reviews.download_as_csv'), :csv) %>
