<h1>Filtered view</h1>
<%= render partial: 'dashboard/search' %>

<h2><%= @message %></h2>

<ul>
    <% if @results.each do |question| %>
        <li><%= question.uin %>: <%= question.question %></li>
    <% end.empty? %>
    	
	<% end %>
</ul>
