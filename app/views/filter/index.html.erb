<h1>Filtered view</h1>
<%= render partial: 'dashboard/search' %>

<h2><%= @message %></h2>

<ul>
    <% if @results.each do |question| %>
        <li><%= question['Uin'] %>: <%= question['Text'] %></li>
    <% end.empty? %>
    	
	<% end %>
</ul>
